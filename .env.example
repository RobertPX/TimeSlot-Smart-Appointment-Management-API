# Local development (Docker)
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/timeslot?schema=public"

# Neon (production) - get this from your Neon dashboard
# DATABASE_URL="postgresql://user:password@ep-xxx.us-east-2.aws.neon.tech/timeslot?sslmode=require"

JWT_SECRET="change-me-to-a-strong-secret"
JWT_REFRESH_SECRET="change-me-to-another-strong-secret"
JWT_EXPIRATION="15m"
JWT_REFRESH_EXPIRATION="7d"

MIN_CANCEL_HOURS=24

PORT=3000
NODE_ENV=development
